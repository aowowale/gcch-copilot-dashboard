# Copy these values into your terminal session, deployment command, or GitHub secrets.
# This file is documentation only. The scripts do not auto-load it.

AZURE_CLOUD=AzureCloud
AZURE_TENANT_ID=<tenant-guid>
AZURE_SUBSCRIPTION_ID=<subscription-guid>
AZURE_RESOURCE_GROUP=rg-example-copilot
AZURE_LOCATION=eastus2
AZURE_STATIC_WEB_APP_NAME=example-copilot-swa
AZURE_STORAGE_ACCOUNT=examplecopilotstorage

# GitHub Actions OIDC secrets
AZURE_CLIENT_ID=<app-registration-client-id>
AZURE_TENANT_ID=<tenant-guid>
AZURE_SUBSCRIPTION_ID=<subscription-guid>

# Example local commands
# az cloud set --name AzureCloud
# az login --tenant <tenant-guid>
# az account set --subscription <subscription-guid>
# ./deploy-azure-swa.ps1 -SubscriptionId <subscription-guid> -ResourceGroup rg-example-copilot -StaticWebAppName example-copilot-swa -Location eastus2
